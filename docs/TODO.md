# next

- [x] `tracker.rs`: тёзка, переживший победителя, прыгает на запомненное место. Проигравший тёзка уходит в `continue` (строки ~155-157) раньше, чем дело дойдёт до слота, и не тратит `awaiting_placement`; когда победитель закрывается, давно открытое окно получает расстановку и уезжает. Расходится с правилом «не воевать с человеком за перетаскивание». Найдено ревью этапа 3, отложено как minor
- [ ] `config.example.yml`: нет блока `mqtt:`, хотя без него не работает подъём окна — он документирован только прозой в README. Тот же класс пробела, что закрыт для блока `state:` коммитом `1eb30d5`
- [ ] `state.rs`: гигиена файлов состояния. `write_atomic` не убирает `*.json.tmp` при отказе записи (итоговый файл при этом цел, но каталог копит мусор), а повторная порча затирает прежний `.bak` — второй сбой уносит уцелевшую копию первого
- [ ] `main.rs`: жалобу `place failed` в строке трея затирает успешное сообщение того же такта. В stderr она остаётся, но трей — единственный канал, где человек видит отказ расстановки
- [ ] ccfzf-picker: `normHost` лежит тремя дословными копиями — `session-windows.js`, `picker-snapshots.js`, `open-transport.js`. Условие рулинга этапа 3 («третья копия где-либо ещё должна стать поводом вынести общий помощник») выполнено
- [ ] ccfzf-picker: `trackerHosts` не проверяет форму элементов `windowHosts` — пустой объект проходит `.filter(Boolean)`, доезжает до `normHost`/`focusPid` и молча даёт нулевой pid, то есть выключает фокус. Файл пишет чужая машина, недоверие к его содержимому уместно
- [ ] ccfzf-picker: `restoreLocally` не покрыт тестом. Сторож проверяет только развилку `snapshotOwner`; пропуск уже открытых сессий и трактовка пустого `sessionIds` как «весь снимок» держатся на одной живой проверке

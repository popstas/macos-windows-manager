# next

- [x] **Плитка: кнопка «разложить окна по основному монитору».** Сделано:
  `crates/mwm-core/src/layout.rs` считает сетку, `Tile windows` в меню трея и
  команда `claude-place` по MQTT просят об одном и том же через один канал.
  Решённое по дороге: колонки переводятся в точки постоянной шириной знака
  (8 pt, константа в ядре); окна собираются на главный экран независимо от
  того, где стояли; полоса меню вычитается постоянным числом, Dock не
  учитывается — рабочую область знает только API главного потока. Всё это
  описано в README, разделе «Раскладки».

- [x] **Каскад: кнопка «сложить окна стопкой».** Сделано: `Cascade windows` в
  меню трея и `mode: "cascade"` в просьбе, окна в половину экрана со сдвигом
  вправо и вниз на высоту заголовка. Хвост решён так: кончилось место на
  ступеньках — стопка начинается заново от угла.

- [ ] **Раскладки не проверены на маке.** Плитка собрана и покрыта тестами, но
  `cargo build --release` идёт только на macOS, а ветка `ax::main_display()`
  живёт под `cfg(target_os = "macos")` и на Linux не компилируется вовсе.
  Проверить на машине: сборку, оба пункта меню, раскладки на одном и на двух
  мониторах, то, что нижний ряд не уезжает под Dock, и что шага каскада хватает
  на заголовок у того терминала, которым пользуются.

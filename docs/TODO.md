# next

- [ ] ccfzf-picker: `normHost` лежит тремя дословными копиями — `session-windows.js`, `picker-snapshots.js`, `open-transport.js`. Условие рулинга этапа 3 («третья копия где-либо ещё должна стать поводом вынести общий помощник») выполнено
- [ ] ccfzf-picker: `trackerHosts` не проверяет форму элементов `windowHosts` — пустой объект проходит `.filter(Boolean)`, доезжает до `normHost`/`focusPid` и молча даёт нулевой pid, то есть выключает фокус. Файл пишет чужая машина, недоверие к его содержимому уместно
- [ ] ccfzf-picker: `restoreLocally` не покрыт тестом. Сторож проверяет только развилку `snapshotOwner`; пропуск уже открытых сессий и трактовка пустого `sessionIds` как «весь снимок» держатся на одной живой проверке

# next

- [ ] **Вкладка Log — здесь и в ccfzf-picker.** Оба приложения пишут только в
  stderr: `eprintln!("mwm: …")` — 22 места в `src-tauri/src` и
  `crates/mwm-core`, у соседа `eprintln!("ccfzf-picker: …")` — 28 по его
  `src-tauri/src`. Запущенное из `.app` stderr уходит в никуда, и на вопрос
  «почему трекер притих» смотреть не во что: ни файла лога, ни буфера в памяти
  нет.

  Машинерия: кольцевой буфер строк в состоянии приложения (`Shared` в
  `main.rs`), функция-обёртка вместо голого `eprintln!`, чтобы строка шла разом
  в stderr и в буфер, команда `read_log` рядом с `load_settings`/`save_settings`
  в `invoke_handler` (`main.rs:951`), и вкладка Log, которая этот буфер
  показывает — опросом или событием Tauri. Время придётся ставить в буфере
  всем строкам: сейчас `chrono::Local::now()` есть ровно у одной
  (`main.rs:434`).

  Задача парная — та же вкладка нужна в ccfzf-picker, и делать их стоит одним
  заходом, пока решение про формат строки и размер буфера в голове.

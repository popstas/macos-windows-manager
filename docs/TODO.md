# next

- [ ] **Настройки вкладками, как в ccfzf-picker.** Вся форма лежит в одном
  `frontend/settings.html` (252 строки, своего модуля под неё нет): `GROUPS` —
  четыре группы (Features, Connection, Windows, MQTT), и `render()` вываливает
  все четыре в `#form` разом. Четырнадцать полей в окно 560×620
  (`open_settings`, `src-tauri/src/main.rs:840`) уже не помещаются — footer
  поэтому и сделан липким. Образец рядом: `PAGES` в
  `../ccfzf-picker/frontend-src/settings-form.js:58` (`id`/`title`/`fields`),
  боковой список `#tabs` шириной 170px, `renderTabs()`/`renderPage()` и CSS
  `.tab`/`.tab.active` в его `settings.html`.

  Подводный камень один, зато обязательный: `collect()` обходит все `GROUPS` и
  берёт `document.getElementById(fieldId(path))`. Отрисуй мы одну вкладку —
  узлов остальных в DOM нет, и `input.checked` падает на `null` при первом же
  Save. Две дороги: собирать патч только с отрисованной вкладки, запоминая
  тронутое при уходе с неё, либо перейти на модель ccfzf-picker целиком —
  множества тронутых полей плюс автосохранение с дебаунсом 400 мс и
  `flushPendingSave()` на переключении вкладки, и тогда кнопка Save не нужна
  вовсе.

  Сторож `the_form_covers_every_field_it_promises`
  (`src-tauri/src/main.rs:1376`) ищет имена ключей подстрокой в тексте
  `settings.html` — переезд групп во вкладки его не ломает, но и не поймает
  поле, которое перестало попадать на экран.

- [ ] **Вкладка Log — здесь и в ccfzf-picker.** Оба приложения пишут только в
  stderr: `eprintln!("mwm: …")` — 22 места в `src-tauri/src` и
  `crates/mwm-core`, у соседа `eprintln!("ccfzf-picker: …")` — 28 по его
  `src-tauri/src`. Запущенное из `.app` stderr уходит в никуда, и на вопрос
  «почему трекер притих» смотреть не во что: ни файла лога, ни буфера в памяти
  нет.

  Машинерия: кольцевой буфер строк в состоянии приложения (`Shared` в
  `main.rs`), функция-обёртка вместо голого `eprintln!`, чтобы строка шла разом
  в stderr и в буфер, команда `read_log` рядом с `load_settings`/`save_settings`
  в `invoke_handler` (`main.rs:951`), и вкладка Log, которая этот буфер
  показывает — опросом или событием Tauri. Время придётся ставить в буфере
  всем строкам: сейчас `chrono::Local::now()` есть ровно у одной
  (`main.rs:434`).

  Задача парная — та же вкладка нужна в ccfzf-picker, и делать их стоит одним
  заходом, пока решение про формат строки и размер буфера в голове.
